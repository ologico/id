---
export const prerender = false;

// Redirect to the generic OAuth link page for GitHub
const redirectUrl = new URL("/app/link-oauth?provider=github", Astro.url.origin);
if (Astro.url.searchParams.get("success")) {
  redirectUrl.searchParams.set("success", "true");
}

return Astro.redirect(redirectUrl.toString());
---
